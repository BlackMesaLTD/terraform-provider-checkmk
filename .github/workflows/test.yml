# Generated by Visual Pipeline Builder
# DO NOT EDIT DIRECTLY - Changes will be overwritten

name: Untitled Pipeline

on:
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
